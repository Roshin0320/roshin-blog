// css 样式规范，后期有需要再加上
module.exports = {
  root: true,
  // 插件
  plugins: ['stylelint-order'],
  // 扩展插件
  extends: ['stylelint-config-standard', 'stylelint-config-prettier'],
  // 规则配置
  rules: {}
};
